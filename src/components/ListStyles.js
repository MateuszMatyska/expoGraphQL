import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    list: {
        flex: 1,
    },
    itemText: {
        color: 'black',
        fontSize: 30
    },
    ItemWrapper: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    } 
});